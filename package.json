{
  "name": "effect-flue",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Flue's Support Agent example, rebuilt in Effect v4 + alchemy-effect. Proves the framework isn't necessary.",
  "scripts": {
    "typecheck": "pnpm -r typecheck",
    "build": "pnpm -r build",
    "deploy": "pnpm --filter @effect-flue/api deploy",
    "dev": "pnpm --filter @effect-flue/api dev"
  },
  "devDependencies": {
    "typescript": "^5.6.0"
  }
}
